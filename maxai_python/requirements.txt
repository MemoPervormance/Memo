# CroixAI — Python dependencies
# Run INSTALL.bat (or: pip install -r requirements.txt)

# Web server
fastapi>=0.110
uvicorn[standard]>=0.27

# ONNX Runtime — DirectML works on ALL Windows GPUs (AMD/Intel/NVIDIA), no CUDA needed
onnxruntime-directml>=1.17
# For NVIDIA CUDA: run INSTALL_CUDA.bat instead

# Numerics / Image
numpy>=1.26
Pillow>=10.0
opencv-python>=4.9

# Config
toml>=0.10
pydantic>=2.0

# Screen capture
dxcam>=0.0.5

# Input / Serial backends
pyserial>=3.5

# Windows APIs (overlay + hotkeys + GDI)
pywin32>=306

# Discord Rich Presence (optional)
pypresence>=4.3

# Optional backends (uncomment to enable):
# windows-capture>=1.2   # WinRT capture (HDR / exclusive fullscreen)
# ndi-python>=0.1        # NDI 2-PC network capture
# pyvigem>=0.0.6         # Virtual Xbox controller

# AI Model Training (only if training your own models):
# ultralytics>=8.2
# torch>=2.2
# torchvision>=0.17
